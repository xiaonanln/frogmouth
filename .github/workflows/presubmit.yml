name: presubmit

on:
  pull_request:
  push:
    branches: [main]

jobs:
  presubmit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Run presubmit checks
        run: python3 scripts/presubmit.py
